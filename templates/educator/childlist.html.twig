{% extends "master.html.twig" %}

{% block content %}

<h3>Group Child List:</h3>

    <table>
        <tr><th>First Name</th><th>Last Name</th><th>Gender</th><th>Date Of Birth</th><th>EmerContactName</th><th>EmergencyPhone</th><th> Note</th><th>Addtendance</th></tr>
        {% for a in list %}
        <tr>
        <td>{{a.firstName}}</td>
        <td>{{a.lastName}}</td>
        <td>{{a.gender}}</td>
        <td>{{a.dateOfBirth}}</td>
        <td>{{a.emergencyContact}}</td>
        <td>{{a.emergencyPhone}}</td>
        <td><form action="/educator/noteHistory">
                <input type="text"  hidden name="id" value="{{a.id}}">
                <input type="submit" value="Note List"></form></a></td>
     
        <td><form action="/educator/attendanceHistory">
                <input type="text" hidden name="id" value="{{a.id}}" >
                <input type="submit" value="Attendance List"></form></a></td>
        </tr>
        {% endfor %}
    </table>
<a href="/educator/educator_board">Back</a> </p>
{% endblock content %}