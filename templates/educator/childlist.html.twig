{% extends "master.html.twig" %}

{% block content %}

<h1 class ="edu"><img src="/images/child.jpg" width="70px"/>Group Child List</h1>
<a class="herf" href="/educator/educator_board"><img src="/images/back.jpg" width="40px"/></a> </p>
    <table class="admintable">
        <tr><th class="th">First Name</th><th class="th" >Last Name</th><th class="th" >Gender</th><th class="th">Date Of Birth</th><th class="th">EmerContactName</th><th class="th">EmergencyPhone</th><th class="th"> Note</th><th class="th">Addtendance</th></tr>
        {% for a in list %}
        <tr>
        <td class="th">{{a.firstName}}</td>
        <td class="th">{{a.lastName}}</td>
        
         <td class="th">{% if a.gender == "F" %}
  <img src="/images/female.png" width="30px"/>
{% else %}
   <img src="/images/male.png" width="30px"/>
{% endif %}</td>
        <td class="th" >{{a.dateOfBirth}}</td>
        <td class="th">{{a.emergencyContact}}</td>
        <td class="th">{{a.emergencyPhone}}</td>
        <td class="th"><form action="/educator/noteHistory">
                <input type="text"  hidden name="id" value="{{a.id}}">
                <input class ="submitList" type="submit" value="Note List"></form></a></td>
     
        <td class="th"><form action="/educator/attendanceHistory">
                <input type="text" hidden name="id" value="{{a.id}}" >
                <input class = "submitList" type="submit" value="Attendance List"></form></a></td>
        </tr>
        {% endfor %}
    </table>

{% endblock content %}